---
import { Image } from "astro:assets";
const { path, alt } = Astro.props;
---

{
  (
    <Image
      src={path}
      alt={alt}
      widths={[560, 980, 1960]}
      sizes={`(min-width: 1280px) 980px, (min-width: 720px) calc(70.37vw + 93px), calc(83.25vw + 17px)`}
    />
  )
}
